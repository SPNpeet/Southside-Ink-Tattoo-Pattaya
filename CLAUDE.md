# sudo-command — กฎประจำโปรเจกต์

เว็บหน้าร้านรับงานของ **Sudo Command** (Tech & Creative Agency)
React + Vite → GitHub Pages ที่ https://spnpeet.github.io/sudo-command/
หน้าเดียว ไม่มี router ไม่มีหลังบ้าน

---

## 1. กฎที่ห้ามละเมิด

### ห้ามแต่งข้อมูลที่พิสูจน์ไม่ได้
ห้ามใส่ผลงานที่ไม่ได้ทำจริง ชื่อลูกค้าที่ยังไม่ได้ขออนุญาต รีวิว หรือตัวเลขอย่าง
"ลูกค้า 100+ ราย" "ยอดขายโต 300%" เด็ดขาด นี่คือเว็บที่ลูกค้าจริงใช้ตัดสินใจจ้างงาน
ถ้าจับได้ว่าแต่ง เสียเครดิตทั้งเว็บ ไม่ใช่แค่บรรทัดนั้น

### ห้ามให้มีปุ่มที่กดแล้วไม่ไปไหน
ทุกอย่างที่ดูกดได้ ต้องกดได้จริง ช่องทางไหนใน `CONTACT` (หัวไฟล์ `src/App.jsx`) ยังว่าง
ปุ่มนั้นต้องไม่ถูกเรนเดอร์ ห้ามใส่ `href="#"` หรือปุ่มหลอกไว้ก่อน

### ฟอร์มติดต่อ ถ้าส่งไม่ได้ ต้องไม่ขึ้น
`CONTACT.web3formsKey` ว่าง = ฟอร์มไม่เรนเดอร์ทั้งอัน
ห้ามทำฟอร์มที่กด "ส่ง" แล้วข้อมูลหายโดยลูกค้าไม่รู้ตัว — เคยเป็นบั๊กจริงมาแล้ว
ลูกค้าจะนั่งรอสายที่ไม่มีวันโทรมา และเราจะไม่มีทางรู้ว่าเสียงานไป

### ห้ามซ่อนเนื้อหาโดยไม่มีทางออก
ถ้าซ่อนอะไรไว้รอ `IntersectionObserver` ต้องมี watchdog ถอดการซ่อนออกเมื่อ observer ไม่ยิง
(ดู `useReveal` ใน `src/hooks.js`) เบราว์เซอร์บางตัว IO ใช้ไม่ได้ ถ้าไม่กันไว้ ลูกค้าเจอหน้าเปล่า

### ห้ามใช้ requestAnimationFrame หน่วงก่อนรันคำสั่งของผู้ใช้
แท็บที่ไม่ได้อยู่หน้าจอจะไม่ยิง rAF คำสั่งจะหายเงียบ ใช้ `setTimeout(fn, 0)` แทน
เคยทำให้ Command Palette กด Enter แล้วไม่เกิดอะไรขึ้นมาแล้ว

---

## 2. ทิศทางดีไซน์

เจ้าของสั่งชัดว่า **"ห้ามให้ดูเหมือน AI ทำเด็ดขาด"** สิ่งที่ทำให้เว็บดูเป็น AI landing page
และห้ามย้อนกลับไปใช้:

- พื้นกรมท่าเข้ม + ตัวหนังสือไล่เฉดม่วง-ฟ้านีออน
- อิโมจิใช้เป็นไอคอน — ใช้ `src/Icon.jsx` ที่วาด SVG เองแทน กริด 24 เส้นหนา 1.5 เท่ากันหมด
- กล่องเทอร์มินัลปลอมในหน้าแรก
- การ์ดมุมมนเรียงกริดเท่ากันหมด hover แล้วลอยขึ้น
- คำโฆษณาลอย ๆ ที่ไม่มีความหมาย เช่น "AI-First" "Fast & Scalable"

ทิศทางที่ใช้อยู่และเจ้าของรับแล้ว: **สิ่งพิมพ์เชิงวิศวกรรม** — กระดาษอุ่น หมึกเข้ม
เส้นไฮไลน์คั่น หัวข้อมีเลขกำกับ สีเน้นสีเดียว (ส้ม `#c93f00`) ตัวหนังสือ IBM Plex Sans Thai

**กลุ่มผู้อ่านคือเจ้าของ SME และสำนักงานบัญชี อายุราว 35-55 ไม่ใช่วัยรุ่น** ทุกการตัดสินใจ
ด้านดีไซน์ให้ชั่งด้วยเกณฑ์นี้: อ่านง่าย คอนทราสต์สูง ดูน่าเชื่อถือ มากกว่าดูหวือหวา

---

## 3. เกณฑ์ที่ต้องผ่านก่อนถือว่าเสร็จ

- `npm run build` ผ่าน และ `npm run lint` exit 0
- คอนทราสต์ตัวหนังสือทุกคู่ผ่าน WCAG AA (4.5:1) ทั้งธีมสว่างและมืด
- เป้ากดบนมือถือไม่ต่ำกว่า 44px ทุกปุ่ม
- ไม่มี horizontal overflow ที่ 375px
- ไม่มีลิงก์ที่ href ว่าง / `tel:` / `mailto:` เปล่า
- ทำงานได้ทั้ง `data-theme` สว่าง มืด และตามระบบ

---

## 4. ข้อควรระวังตอนทดสอบใน Browser pane

ถ้า pane ไม่ได้ถูกแสดงผล (screenshot error ว่า not compositing) จะเจอ:
`IntersectionObserver` ไม่ยิงเลยแม้แต่รอบแรก, `requestAnimationFrame` ไม่ยิง,
`scrollIntoView({behavior:'smooth'})` ไม่ขยับ (แต่ `'instant'` ขยับปกติ)

วิธีแยกว่าเป็นเครื่องมือหรือโค้ด: สร้าง `IntersectionObserver` เปล่า ๆ จับ element ที่อยู่ในจอ
ถ้ายิง 0 ครั้ง = เครื่องมือพัง

React state ที่เพิ่งเปลี่ยนจาก `.click()` ต้องอ่านใน tool call **ถัดไป**
อ่านในคอลเดียวกันจะได้ค่าเก่าเสมอ

---

## 5. งานที่ผู้ช่วยทำแทนไม่ได้ ต้องให้เจ้าของทำเอง

อย่าพยายามทำเอง อย่าเดาค่าใส่ ให้บอกเจ้าของแล้วรอ:

- **สมัครบัญชี web3forms.com** เพื่อเอา access key (ต้องเป็นรูปแบบ UUID)
- **ลิงก์ LINE OA** และ **เบอร์โทรจริง** — เบอร์ในเพจเฟซบุ๊กเป็น `+61 ...` ซึ่งผิด โทรไม่ติด
- **รูปผลงาน** — ดึงจากเพจเองไม่ได้เพราะต้องล็อกอิน ต้องให้ส่งไฟล์มา
- **แก้ไขเพจเฟซบุ๊ก** — ต้องขออนุญาตเฉพาะเจาะจงทุกครั้ง

---

## 6. คำสั่งที่ใช้บ่อย

```bash
npm run dev      # เซิร์ฟเวอร์ระหว่างพัฒนา
npm run build    # build ลง dist/
npm run preview  # ดู dist/ จริง ที่ /sudo-command/
npm run lint     # oxlint
```

deploy อัตโนมัติเมื่อ push ขึ้น `main` ผ่าน `.github/workflows/deploy.yml`
`vite.config.js` ต้องคง `base: '/sudo-command/'` ไว้เสมอ ไม่งั้น asset 404 ทั้งเว็บ

ตรวจผลรัน workflow จาก API (`/actions/runs` ดู `conclusion`)
อย่าเชื่อหน้า Actions ที่ render มา เคยสรุปผิดว่า success ทั้งที่ล้มเหลว
